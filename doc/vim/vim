1. 配置

    配置文件

        ~/.vimrc    #  echo $MYVIMRC

    运行时路径

        ~/.vim      # set runtimepath, 插件安装位置

2. 常用命令

    查看文件的十六进制代码

    :%!xxd
        %    全局操作
	!    调用第三方软件
	xxd  将文件转储为十六进制

    举例:
    	vim -b a.out	# 打开二进制文件
	:%!xxd -g 1	# 转换为 16 进制视图, 一组一组显示文件数组, 每组字节数为 1
