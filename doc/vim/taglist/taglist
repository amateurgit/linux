安装

   cd ~/.vim
   unzip taglist_46.zip
   rm unzip taglist_46.zip
   cd doc
   :helptags .			  # 打开 vim, 执行此命令, 加入帮助主题

配置

    let Tlist_Show_One_File=1	  # 只显示当前的文件的标签
    let Tlist_Exit_OnlyWindow=1   # taglist 是最后一个窗口时,退出 vim

使用

   :TlistToggle 		  # 打开代码源文件, 执行此命令
