multiprogramming

    程序暂时无须使用 CPU, 监控程序启动另外等待程序。

    调度策略粗糙, 程序不分轻重缓急, 人机交互不友好。

time-sharing system

    程序执行一段时间, 主动让出 CPU
  
    程序死循环, 或进行很耗时运算, 霸占 CPU 不让, 其他程序无法执行。

multi-tasking

    操作系统接管硬件资源, 程序以进程为单位运行, 运行等级比操作系统低, 由操作系统统一调度。
